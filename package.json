{
  "name": "fastify-first-try",
  "version": "0.0.5",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm run build && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rynvva",
  "license": "MIT",
  "dependencies": {
    "fastify": "3.11.0"
  }
}
